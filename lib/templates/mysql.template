## MYSQL 
  mysql:
    container_name: ${PROFILE}_mysql
    image: $KUSANGI_MARIADB_IMAGE
    user: "999"
    env_file:
      - .kusanagi.mysql
    network_mode: "service:httpd"
    volumes:
      - database:/var/run/mysqld
      - database:/var/lib/mysql
 
